# DeoVR Library 使用手册

版本：1.1（仅 CLI）  
目录：`deovr-library/`

---

## 1. 这是什么

把本地媒体库做成：

1. **网页片库**：按演员、类型、片商、2D/VR 筛选  
2. **DeoVR 服务**：头显/PC 内置浏览器打开列表并播放  
3. **播放**：
   - `.strm`：读取其中 HTTP 地址，可把 `127.0.0.1` 改写成局域网 IP 后 302  
   - 本地视频（mp4/mkv…）：由本服务 `/play/{id}` 直接 Range 输出  

网页是动态读 SQLite 的。加片后执行 `scan` 即可，不必「重新生成网站」。

---

## 2. 支持的媒体格式

| 类型 | 扩展名 | 播放方式 |
|------|--------|----------|
| 流媒体指针 | `.strm` | 302 到 STRM 内 URL（可改写/解析 CDN） |
| 本地视频 | `.mp4` `.mkv` `.avi` `.m4v` `.mov` `.wmv` `.ts` `.m2ts` `.webm` `.flv` `.mpg` `.mpeg` | 本服务直出文件 |

同一目录、同一文件名（stem）若同时存在 `.strm` 与视频文件，**优先使用 `.strm`**。

扩展名可在 `data/config.json` 的 `video_extensions` 中增删。

---

## 3. NFO 兼容性（Emby / Jellyfin / Kodi）

三者共用同一套 Kodi 风格 XML，本工具均支持：

| 根节点 | 用途 |
|--------|------|
| `<movie>` | 电影（Emby/Jellyfin/Kodi） |
| `<episodedetails>` / `<episode>` | 分集 |
| `<tvshow>` / `<series>` | 剧集信息（扫描时优先用同名分集 nfo；`tvshow.nfo` 作兜底较少） |

已解析字段：

- 标题：`title` / `originaltitle` / `sorttitle` / `showtitle`  
- 简介：`plot` / `outline` / `tagline`  
- 片商：`studio`（多个取第一个）  
- 日期：`year` / `aired` / `premiered` / `releasedate` / `dateadded`  
- 评分：简单 `<rating>`，以及 Emby/Jellyfin 的 `<ratings><rating><value>`  
- 时长：`<runtime>`（分钟），或 `durationinseconds`  
- 演员：`<actor><name>`  
- 类型：`<genre>` / `<tag>`  
- 外部 ID：`<uniqueid type="imdb">` / `tmdb` 等，或从标题提取  


封面优先级（节选）：`{stem}-poster.jpg` → `{stem}.jpg` → `poster.jpg` → `folder.jpg` → `fanart.jpg` 等（含 png/webp）。

---

## 4. 目录结构示例

```
Movies/
  Inception (2010)/
    Inception (2010).mp4          # 或 .strm
    Inception (2010).nfo          # / movie.nfo
    Inception (2010)-poster.jpg
    fanart.jpg

  The Matrix (1999)/
    The Matrix (1999).mkv
    movie.nfo
    poster.jpg

Movies-VR/
  Escape Room VR/
    Escape Room VR-E01.mp4
    Escape Room VR-E01.nfo
    Escape Room VR-E01-poster.jpg
```

`.strm` 内容示例（一行 HTTP 媒体地址）：

```
http://127.0.0.1:8096/Videos/abc123/stream.mp4
```

---

## 5. 安装

```bash
cd deovr-library
python3 -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt
```

之后每次：

```bash
source .venv/bin/activate
```

---

## 6. 命令行（全部操作）

### 6.0 空白 Demo（GitHub 测试）

```bash
python run_cli.py demo              # 重置并用 testdata 扫描
python run_cli.py demo --serve      # 扫描后直接启动服务
python scripts/smoke_test.py        # API 冒烟测试
```

### 6.1 初始化

```bash
python run_cli.py init              # 补全默认项（保留已有目录）
python run_cli.py init --reset      # 清空配置+数据库+封面缓存，重新开始
```

### 6.2 媒体目录

```bash
# 添加
python run_cli.py library add --path "/path/to/Movies" --name Movies --kind 2d
python run_cli.py library add --path "/path/to/Movies-VR" --name Movies-VR --kind vr

# 列出 / 删除（按 name 或 path）
python run_cli.py library list
python run_cli.py library remove Movies
```

`kind`：`2d` 或 `vr`（影响 DeoVR 投影：flat / dome+sbs）。

### 6.3 扫描入库

```bash
python run_cli.py scan
python run_cli.py scan --force          # 忽略增量，全量重扫
python run_cli.py scan --path "/path" --name Temp --kind 2d   # 临时扫一个目录
```

### 6.4 改地址（STRM 里的 127.0.0.1）

头显无法访问电脑的 `127.0.0.1`，需改成局域网 IP（如 `192.168.0.18`）：

```bash
python run_cli.py config --detect-ip
python run_cli.py config --rewrite --rewrite-to 192.168.0.18 --rewrite-from 127.0.0.1,192.168.0.16 --no-resolve-cdn
python run_cli.py config --show-only
```

效果：

```
http://127.0.0.1:8096/Videos/.../stream.mp4
→ http://192.168.0.18:8096/Videos/.../stream.mp4

http://192.168.0.16:12366/play115/...
→ http://192.168.0.18:12366/play115/...
```

可选：`--resolve-cdn` 让服务端先跟随跳转到最终 CDN/直链再交给播放器。

### 6.5 启动服务

```bash
python run_cli.py serve
python run_cli.py serve --port 8765 --rewrite --rewrite-to 192.168.0.18 --save-config
```

---

## 7. 访问地址

| 用途 | URL |
|------|-----|
| 网页浏览（推荐） | `http://<IP>:8765/browse` |
| 打开 DeoVR（推荐） | `http://<IP>:8765/open` → 点「用 DeoVR 打开」 |
| DeoVR JSON 入口 | `http://<IP>:8765/deovr` 或 `deovr://http://<IP>:8765/deovr` |
| DeoVR 按 2D/VR | `http://<IP>:8765/deovr/kind/vr` |
| DeoVR 按类型 | `http://<IP>:8765/deovr/g/{id}` |
| DeoVR 按演员 | `http://<IP>:8765/deovr/a/{id}` |
| 单片 JSON | `http://<IP>:8765/deovr/{id}` |
| 播放 | `http://<IP>:8765/play/{id}` |
| 封面 | `http://<IP>:8765/cover/{id}.jpg` |

**重要**：

1. **网页筛选**：浏览器打开 `http://<IP>:8765/browse`，用顶部下拉选类型/演员。  
2. **DeoVR 片库**：打开 `http://<IP>:8765/open` 点「用 DeoVR 打开」，或使用 `deovr://http://<IP>:8765/deovr`。底栏 Tab（Recent / 目录 / 2D / VR / 热门类型）**直接就是影片列表**——用底栏切换类型即可，不要点进「文件夹」。DeoVR 不支持网页那种下拉筛选。  
3. **网页点类型标签**：会跳到 `/browse?genre=名称` 并自动选中该类型；请用 `/browse`，不要依赖根路径 `/`。

---

## 8. 加片后如何更新

1. 把新文件夹放进已添加的根目录  
2. `python run_cli.py scan`  
3. 刷新网页 / 重新打开 DeoVR  

一般**不必**重启 `serve`。增量扫描会跳过未变更文件。

---

## 9. 配置文件

`data/config.json` 主要字段：

| 字段 | 含义 |
|------|------|
| `libraries` | 目录列表 `name` / `path` / `kind` |
| `host` / `port` | 监听地址 |
| `video_extensions` | 本地视频后缀 |
| `rewrite_localhost_enabled` | 是否改写播放地址主机 |
| `rewrite_to` | 改写目标 IP |
| `rewrite_from` | 要替换的源主机列表（如 `127.0.0.1`、`192.168.0.16`） |
| `resolve_strm_redirects` | 是否解析到 CDN |
| `vr_screen_type` / `vr_stereo_mode` | VR 投影（默认 dome / sbs） |
| `flat_screen_type` / `flat_stereo_mode` | 2D（默认 flat / off） |

数据：`data/library.db`（SQLite）、`data/thumbs/`（封面缩略图缓存）。

---

## 10. 常见问题

### 播不了（STRM）

- 确认媒体源服务（如 Jellyfin/Emby/网盘直链网关）在跑，且对局域网开放  
- `config --rewrite --rewrite-to <局域网IP>`  
- 浏览器打开 `/api/resolve/{id}` 看 `final` 是否可访问  

### 本地 mp4 播不了

- 确认已 `scan` 入库  
- 直接访问 `/play/{id}` 是否能下/播  
- DeoVR 需能访问电脑的 `8765` 端口  

### 封面网页有、DeoVR 没有

- 使用带扩展名地址：`/cover/{id}.jpg`（默认已写入 DeoVR JSON）  
- 重新打开 `/deovr`，避免旧缓存  

### NFO 演员为空

- 补全 `<actor><name>` 后 `--force` 重扫  
- 否则会尝试从标题末尾猜测  

### VR 左右/上下不对

- 改 `vr_stereo_mode`（`sbs` ↔ `tb`）、`vr_screen_type` 后重启 serve  

---

## 11. 快速对照

```
初始化        python run_cli.py init
重置全部      python run_cli.py init --reset
加目录        python run_cli.py library add --path DIR --name NAME --kind 2d|vr
改地址        python run_cli.py config --rewrite --rewrite-to 192.168.0.18
扫描          python run_cli.py scan
启动          python run_cli.py serve --port 8765
网页          http://127.0.0.1:8765/
DeoVR         http://<局域网IP>:8765/deovr
```
