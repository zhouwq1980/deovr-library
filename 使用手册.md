# DeoVR Library 使用手册

版本：1.2（仅 CLI）  
仓库：https://github.com/zhouwq1980/deovr-library  
安装目录（一键安装默认）：`~/deovr-library`

---

## 目录

1. [这是什么](#1-这是什么)
2. [环境要求](#2-环境要求)
3. [支持的媒体格式](#3-支持的媒体格式)
4. [NFO 兼容性](#4-nfo-兼容性emby--jellyfin--kodi)
5. [目录结构示例](#5-目录结构示例)
6. [安装](#6-安装)
7. [媒体目录：增删改查](#7-媒体目录增删改查)
8. [扫描入库](#8-扫描入库)
9. [改地址（STRM）](#9-改地址strm)
10. [启动服务与访问](#10-启动服务与访问)
11. [加片后如何更新](#11-加片后如何更新)
12. [配置文件](#12-配置文件)
13. [命令行全集](#13-命令行全集)
14. [常见问题](#14-常见问题)
15. [快速对照](#15-快速对照)

---

## 1. 这是什么

把本地媒体库做成：

1. **网页片库**：按演员、类型、片商、2D/VR 筛选  
2. **DeoVR 服务**：头显/PC 内置浏览器打开列表并播放  
3. **播放**：
   - `.strm`：读取其中 HTTP 地址，可把本机/局域网主机改写成 `rewrite_to` 后 302  
   - 本地视频（mp4/mkv…）：由本服务 `/play/{id}` 直接 Range 输出  

网页动态读 SQLite。加片后执行 `scan` 即可，不必「重新生成网站」。

个人数据（片库路径、数据库、封面缓存）只在本机，**不会**进 GitHub。

---

## 2. 环境要求

| 项目 | 要求 |
|------|------|
| 系统 | macOS 推荐（`install.sh` 按 macOS 设计）；Linux/Windows 可手动 venv |
| Python | **≥ 3.9** |
| 工具 | `curl`；远程安装还需 `git` 或 `unzip` |
| 网络 | 电脑与头显同一局域网；安装时能访问 GitHub |

检查 Python：

```bash
python3 --version   # 应 ≥ 3.9
# 不足时：brew install python
```

---

## 3. 支持的媒体格式

| 类型 | 扩展名 | 播放方式 |
|------|--------|----------|
| 流媒体指针 | `.strm` | 302 到 STRM 内 URL（可改写 / 可选解析 CDN） |
| 本地视频 | `.mp4` `.mkv` `.avi` `.m4v` `.mov` `.wmv` `.ts` `.m2ts` `.webm` `.flv` `.mpg` `.mpeg` | 本服务直出文件 |

同一目录、同一文件名（stem）若同时存在 `.strm` 与视频文件，**优先使用 `.strm`**。

扩展名可在 `data/config.json` 的 `video_extensions` 中增删。

---

## 4. NFO 兼容性（Emby / Jellyfin / Kodi）

三者共用同一套 Kodi 风格 XML，本工具均支持：

| 根节点 | 用途 |
|--------|------|
| `<movie>` | 电影 |
| `<episodedetails>` / `<episode>` | 分集 |
| `<tvshow>` / `<series>` | 剧集信息（扫描时优先同名分集 nfo） |

已解析字段：

- 标题：`title` / `originaltitle` / `sorttitle` / `showtitle`  
- 简介：`plot` / `outline` / `tagline`  
- 片商：`studio`（多个取第一个）  
- 日期：`year` / `aired` / `premiered` / `releasedate` / `dateadded`  
- 评分：简单 `<rating>`，以及 `<ratings><rating><value>`  
- 时长：`<runtime>`（分钟），或 `durationinseconds`  
- 演员：`<actor><name>`  
- 类型：`<genre>` / `<tag>`  
- 外部 ID：`<uniqueid type="imdb">` / `tmdb` 等  

封面优先级（节选）：`{stem}-poster.jpg` → `{stem}.jpg` → `poster.jpg` → `folder.jpg` → `fanart.jpg` 等（含 png/webp）。

---

## 5. 目录结构示例

```
Movies/
  Inception (2010)/
    Inception (2010).mp4          # 或 .strm
    Inception (2010).nfo          # 或 movie.nfo
    Inception (2010)-poster.jpg
    fanart.jpg

  The Matrix (1999)/
    The Matrix (1999).mkv
    movie.nfo
    poster.jpg

Movies-VR/
  Escape Room VR/
    Escape Room VR-E01.mp4
    Escape Room VR-E01.nfo
    Escape Room VR-E01-poster.jpg
```

`.strm` 内容示例（一行 HTTP 媒体地址）：

```
http://127.0.0.1:8096/Videos/abc123/stream.mp4
```

仓库自带空白示例：`testdata/Movies`、`testdata/Movies-VR`（假 STRM，无真实影片）。

---

## 6. 安装

### 6.1 macOS 一键安装（推荐）

默认安装到 `~/deovr-library`（可用 `--dir` 或环境变量 `DEOVR_LIBRARY_HOME` 改）。

```bash
# Demo + 启动
curl -fsSL https://raw.githubusercontent.com/zhouwq1980/deovr-library/main/install.sh | bash -s -- --demo --serve

# 接入自己的片库
curl -fsSL https://raw.githubusercontent.com/zhouwq1980/deovr-library/main/install.sh | bash -s -- \
  --2d ~/Movies/2D --vr ~/Movies/VR --rewrite-to 192.168.0.34 --serve

# 仅更新代码/依赖，不扫描
curl -fsSL https://raw.githubusercontent.com/zhouwq1980/deovr-library/main/install.sh | bash -s -- --skip-scan
```

**重要**：传参必须用 `bash -s --`，例如：

```bash
# ✅
curl .../install.sh | bash -s -- --demo --serve
# ❌ 参数进不了脚本
curl .../install.sh | bash --demo --serve
```

`install.sh` 选项：

| 选项 | 含义 |
|------|------|
| `--2d DIR` | 设置固定 2D 目录 |
| `--vr DIR` | 设置固定 VR 目录 |
| `--clear-2d` / `--clear-vr` | 清除固定槽位 |
| `--rewrite-to IP` | STRM 改写目标 IP |
| `--port N` | 服务端口（默认 8765） |
| `--reset` | 安装前清空配置/库/封面 |
| `--demo` | 使用 `testdata` |
| `--serve` | 装完启动服务 |
| `--skip-scan` | 不扫描 |
| `--dir PATH` | 安装目录 |
| `--python PATH` | 指定 Python |
| `--remote` | 强制远程拉取到 `--dir` |

已存在目录时会尝试 `git pull` 更新后再装依赖。

本地已 clone：

```bash
cd deovr-library
chmod +x install.sh
./install.sh --2d ~/Movies/2D --vr ~/Movies/VR --serve
```

### 6.2 手动安装

```bash
git clone https://github.com/zhouwq1980/deovr-library.git
cd deovr-library
python3 -m venv .venv
source .venv/bin/activate          # Windows: .venv\Scripts\activate
pip install -r requirements.txt
python run_cli.py init
```

之后每次：

```bash
cd ~/deovr-library && source .venv/bin/activate
```

### 6.3 空白 Demo

```bash
python run_cli.py demo              # 重置并用 testdata 扫描
python run_cli.py demo --serve      # 扫描后直接启动
python scripts/smoke_test.py        # API 冒烟测试（需已 serve）
```

清空重来：

```bash
python run_cli.py init --reset
```

---

## 7. 媒体目录：增删改查

配置存在 `data/config.json` 的 `libraries` 数组。每项：`name` / `path` / `kind`（`2d` 或 `vr`）。

### 7.1 列出

```bash
python run_cli.py library list
```

示例输出：

```
1. 2D [2d] /Users/你/AV-2D
2. VR [vr] /Users/你/AV-VR
--- 固定槽位 ---
  2D: /Users/你/AV-2D
  VR: /Users/你/AV-VR
```

### 7.2 固定 2D / VR 槽位（推荐）

每个 kind **只保留一个**固定槽位（名称固定为 `2D` / `VR`）。再次 `set` 会覆盖旧路径。

```bash
# 增 / 改
python run_cli.py library set-2d --path "/Users/你/AV-2D"
python run_cli.py library set-vr --path "/Users/你/AV-VR"

# 删整个固定槽位（清除该 kind）
python run_cli.py library clear-2d
python run_cli.py library clear-vr
```

### 7.3 按名称或路径删除（重点）

语法是**子命令** `remove`，后面跟**一个位置参数**（名称 **或** 完整路径）：

```bash
# ✅ 按列表里的 name 删除
python run_cli.py library remove 2D
python run_cli.py library remove AV-2D
python run_cli.py library remove VR

# ✅ 按完整 path 删除（路径含空格时请加引号）
python run_cli.py library remove "/Users/z/Desktop/工作/claw/deovr-library/testdata/Movies"
```

**常见写错：**

```bash
# ❌ 不要用 --remove
python run_cli.py library --remove 2D

# ❌ 不要写成 --path
python run_cli.py library --remove --path /xxx

# ❌ 不要写成 library remove --name 2D（当前没有 --name）
```

正确帮助：

```bash
python run_cli.py library remove -h
# usage: ... library remove [-h] name
```

删完后：

```bash
python run_cli.py library list
python run_cli.py scan --force
```

### 7.4 添加任意多个目录

固定槽位之外，还可 `add` 多个自定义名称目录：

```bash
python run_cli.py library add --path "/path/to/Extra" --name Extra --kind 2d
python run_cli.py library add --path "/path/to/Extra-VR" --name Extra-VR --kind vr
python run_cli.py library remove Extra
```

同名 `add` 会覆盖该项。`kind` 影响 DeoVR 投影：`2d` → flat；`vr` → dome + sbs（可在配置改）。

### 7.5 建议工作流

1. 只用 `set-2d` / `set-vr` 管理两个主目录  
2. 多余条目用 `library remove 名称` 删掉  
3. `scan --force` 重建索引  
4. `serve` 对外提供服务  

---

## 8. 扫描入库

```bash
python run_cli.py scan                 # 增量（跳过未变更）
python run_cli.py scan --force         # 全量重扫（改目录/改 NFO 后建议用）
python run_cli.py scan --path "/path" --name Temp --kind 2d   # 临时扫一个目录
```

扫描结果会写入 `data/library.db`，封面缩略图缓存到 `data/thumbs/`。

---

## 9. 改地址（STRM）

头显无法访问电脑的 `127.0.0.1`，也常需要把 STRM 里其它局域网 IP 统一成电脑当前 IP。

```bash
python run_cli.py config --detect-ip
python run_cli.py config --rewrite --rewrite-to 192.168.0.18
python run_cli.py config --show-only
```

开启后会自动改写 STRM 中的本机/RFC1918 私网主机到 `rewrite_to`；公网 CDN 一般不动。  
可选：`--resolve-cdn` 让服务端先跟随跳转到最终直链再交给播放器。

效果示例：

```
http://127.0.0.1:8096/Videos/.../stream.mp4
→ http://192.168.0.18:8096/Videos/.../stream.mp4

http://192.168.0.16:12366/play115/...
→ http://192.168.0.18:12366/play115/...
```

`rewrite_from` 可选，仅当你需要额外改写某些主机名时才填。

---

## 10. 启动服务与访问

```bash
python run_cli.py serve
python run_cli.py serve --port 8765 --rewrite --rewrite-to 192.168.0.18 --save-config
```

| 用途 | URL |
|------|-----|
| 网页浏览（推荐） | `http://<IP>:8765/browse` |
| 打开 DeoVR（推荐） | `http://<IP>:8765/open` →「用 DeoVR 打开」 |
| DeoVR JSON 入口 | `http://<IP>:8765/deovr` 或 `deovr://http://<IP>:8765/deovr` |
| DeoVR 按 2D/VR | `http://<IP>:8765/deovr/kind/vr` |
| DeoVR 按类型 | `http://<IP>:8765/deovr/g/{id}` |
| DeoVR 按演员 | `http://<IP>:8765/deovr/a/{id}` |
| 单片 JSON | `http://<IP>:8765/deovr/{id}` |
| 播放 | `http://<IP>:8765/play/{id}` |
| 封面 | `http://<IP>:8765/cover/{id}.jpg` |

**使用注意：**

1. **网页**：打开 `/browse`，用顶部下拉筛选。类型标签会跳到 `/browse?genre=名称`。  
2. **DeoVR**：务必用 `/open` 或 `deovr://...`，不要在 VR 浏览器里直接盯着原始 JSON。底栏 Tab（Recent / 目录 / 2D / VR / 类型）本身就是影片列表。  
3. **头显**：URL 里用局域网 IP，不要用 `127.0.0.1`。电脑防火墙需放行端口（默认 8765）。

---

## 11. 加片后如何更新

1. 把新文件夹放进已配置的根目录  
2. `python run_cli.py scan`（大改用 `--force`）  
3. 刷新网页 / 重新打开 DeoVR  

一般**不必**重启 `serve`。

---

## 12. 配置文件

主配置：`data/config.json`（本机私有，不进 git）。  
模板：`data/config.example.json`。

| 字段 | 含义 |
|------|------|
| `libraries` | 目录列表 `name` / `path` / `kind` |
| `host` / `port` | 监听地址 |
| `video_extensions` | 本地视频后缀 |
| `rewrite_localhost_enabled` | 是否改写播放地址主机 |
| `rewrite_to` | 改写目标 IP（一般只配这个） |
| `rewrite_from` | 可选额外主机名 |
| `resolve_strm_redirects` | 是否解析到 CDN |
| `vr_screen_type` / `vr_stereo_mode` | VR 投影（默认 dome / sbs） |
| `flat_screen_type` / `flat_stereo_mode` | 2D（默认 flat / off） |

数据文件：

| 路径 | 说明 |
|------|------|
| `data/library.db` | SQLite 索引（含 FTS） |
| `data/thumbs/` | 封面缩略图缓存 |

---

## 13. 命令行全集

```bash
python run_cli.py -h
python run_cli.py library -h
python run_cli.py library remove -h
```

| 命令 | 作用 |
|------|------|
| `init` / `init --reset` | 初始化；重置清空全部本地数据 |
| `demo` / `demo --serve` | testdata 示例片库 |
| `library list` | 列出目录 |
| `library set-2d --path DIR` | 设置/覆盖固定 2D |
| `library set-vr --path DIR` | 设置/覆盖固定 VR |
| `library clear-2d` / `clear-vr` | 清除固定槽位 |
| `library add --path DIR [--name N] [--kind 2d\|vr]` | 添加目录 |
| `library remove NAME_OR_PATH` | **按名称或路径删除** |
| `scan` / `scan --force` | 扫描入库 |
| `config --detect-ip` | 检测局域网 IP |
| `config --rewrite --rewrite-to IP` | 启用改写 |
| `config --show-only` | 只显示配置 |
| `serve [--port N] [--rewrite ...]` | 启动 HTTP |

---

## 14. 常见问题

### `library --remove` 报 invalid choice

正确写法是子命令，不是 flag：

```bash
python run_cli.py library remove 2D
python run_cli.py library remove "/完整/路径"
```

### 播不了（STRM）

- 确认媒体源（Jellyfin/Emby/网盘网关）在跑且对局域网开放  
- `config --rewrite --rewrite-to <局域网IP>`  
- 浏览器打开 `/api/resolve/{id}` 看 `final` 是否可访问  

### 本地 mp4 播不了

- 确认已 `scan` 入库  
- 直接访问 `/play/{id}`  
- DeoVR 需能访问电脑的 `8765` 端口  

### 封面网页有、DeoVR 没有

- 使用 `/cover/{id}.jpg`（默认已写入 DeoVR JSON）  
- 重新打开 `/deovr`，清旧缓存  

### NFO 演员为空

- 补全 `<actor><name>` 后 `scan --force`  
- 否则会尝试从标题末尾猜测  

### VR 左右/上下不对

- 改 `vr_stereo_mode`（`sbs` ↔ `tb`）、`vr_screen_type` 后重启 serve  

### 扫描报错 / 想彻底重来

```bash
python run_cli.py init --reset
python run_cli.py library set-2d --path "/真实目录"
python run_cli.py library set-vr --path "/真实VR目录"
python run_cli.py config --rewrite --rewrite-to <局域网IP>
python run_cli.py scan --force
python run_cli.py serve --port 8765
```

### curl 安装后找不到命令

```bash
cd ~/deovr-library
source .venv/bin/activate
python run_cli.py library list
```

---

## 15. 快速对照

```
一键安装      curl -fsSL https://raw.githubusercontent.com/zhouwq1980/deovr-library/main/install.sh | bash -s -- --demo --serve
本地安装      ./install.sh --2d DIR --vr DIR --serve
初始化        python run_cli.py init
重置全部      python run_cli.py init --reset
Demo          python run_cli.py demo --serve
列出目录      python run_cli.py library list
设2D/VR       python run_cli.py library set-2d|set-vr --path DIR
清2D/VR       python run_cli.py library clear-2d|clear-vr
删目录        python run_cli.py library remove 名称或完整路径
加目录        python run_cli.py library add --path DIR --name NAME --kind 2d|vr
改地址        python run_cli.py config --rewrite --rewrite-to 192.168.0.18
扫描          python run_cli.py scan --force
启动          python run_cli.py serve --port 8765
网页          http://127.0.0.1:8765/browse
DeoVR引导     http://<局域网IP>:8765/open
DeoVR协议     deovr://http://<局域网IP>:8765/deovr
```
